# API Configuration
# URL where the backend API is running
# 
# Default: http://localhost:3000
# You only need to create a .env file if you want to use a different URL/port
# 
# For Docker Compose: use http://localhost:3000 (the exposed port on the host)
# For local development: use http://localhost:3000 (or your backend port)
VITE_API_URL=http://localhost:3000
